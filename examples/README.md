# Примеры Effector: Приоритеты очередей и getState vs Sample

Коллекция практических примеров, демонстрирующих работу системы приоритетов очередей в Effector и проблемы с использованием `getState()` в эффектах.

## 📚 Примеры по приоритетам очередей

### 1. priority-example-1.js - Базовые операции
**Что показывает:**
- Сравнение `on` (pure приоритет) vs `sample` (sampler приоритет)
- Порядок выполнения при одновременном обновлении стора и sample
- Демонстрация, что `on` выполняется ДО всех `watch`

**Запуск:**
```bash
node examples/priority-example-1.js
```

**Ключевые моменты:**
- Pure (1) → Sampler (4) → Effect (5)
- Sample всегда видит обновленное значение стора

---

### 2. priority-example-2.js - Стор + Эффект через Sample
**Что показывает:**
- Обновление стора и вызов эффекта через один `sample`
- Детальный порядок выполнения с эффектами (`pending`, `done`, `watch`)
- Разница между `batch: true` (по умолчанию) и `batch: false`

**Запуск:**
```bash
node examples/priority-example-2.js
```

**Ключевые моменты:**
- Sample гарантирует, что эффект получит актуальные данные
- Batch режим оптимизирует множественные обновления

---

### 3. priority-example-3-complex.js - Все приоритеты
**Что показывает:**
- Демонстрация всех 6 уровней приоритетов в одном сценарии
- Цепочка `forward` (child приоритет - deprecated)
- Разница между `combine` (barrier) и `sample` (sampler)

**Запуск:**
```bash
node examples/priority-example-3-complex.js
```

**Ключевые моменты:**
- Child (0) → Pure (1) → Read (2) → Barrier (3) → Sampler (4) → Effect (5)
- Combine выполняется РАНЬШЕ чем sample

---

## ⚠️ Примеры: getState() vs Sample

### 4. priority-example-4-getState-vs-sample.js - Сравнение подходов
**Что показывает:**
- Три основные проблемы с `getState()`:
  1. Race condition при быстрых обновлениях
  2. Несогласованность данных в async/await
  3. Проблемы с множественными зависимостями

**Запуск:**
```bash
node examples/priority-example-4-getState-vs-sample.js
```

**Ключевые моменты:**
- getState() читает ТЕКУЩЕЕ состояние, не снимок
- Sample создает неизменяемый снимок данных

---

### 5. ❌ priority-example-6-incorrect-getState.js - НЕКОРРЕКТНОЕ использование
**Что показывает:**
- **Реальные критические баги** с `getState()`
- Race condition: баланс уходит в **минус** (-200₽)
- Асинхронная несогласованность в эффектах
- Несинхронизированное чтение множественных сторов

**Запуск:**
```bash
node examples/priority-example-6-incorrect-getState.js
```

**⚠️ ВНИМАНИЕ:** Этот пример показывает **НЕПРАВИЛЬНЫЙ** код!

**Ключевые проблемы:**
```javascript
// ❌ ПЛОХО
const transferFx = createEffect(async (amount) => {
  const balance = $balance.getState() // Читаем состояние

  if (balance >= amount) {
    await delay()
    deductBalance(amount) // Баланс мог измениться!
  }
})
```

**Результат:** Две транзакции по 600₽ при балансе 1000₽ → баланс = **-200₽** 💥

---

### 6. ✅ priority-example-7-correct-sample.js - КОРРЕКТНОЕ использование
**Что показывает:**
- **Правильное решение** через `sample`
- Консистентность данных в async операциях
- Безопасные транзакции без race conditions
- **Бонус:** Строгий транзакционный паттерн с валидацией

**Запуск:**
```bash
node examples/priority-example-7-correct-sample.js
```

**✅ Правильный код:**
```javascript
// ✅ ХОРОШО
const transferFx = createEffect(async ({amount, balance}) => {
  // balance - это СНИМОК, не изменится

  if (balance >= amount) {
    await delay()
    deductBalance(amount)
  }
})

sample({
  source: $balance,
  clock: transfer,
  fn: (balance, amount) => ({balance, amount}),
  target: transferFx
})
```

**Результат:** Безопасная обработка транзакций, вторая отклонена → баланс = **400₽** ✅

---

## 📖 Документация

### priority-cheatsheet.md - Шпаргалка по приоритетам
Подробная справка по системе приоритетов:
- Таблица всех 6 приоритетов
- Визуальная схема порядка выполнения
- Практические примеры с объяснениями
- Частые ошибки и как их избежать

### getState-vs-sample-guide.md - Полное руководство
Исчерпывающее руководство по выбору между getState() и sample:
- Когда getState() **опасен** (race conditions, async, транзакции)
- Когда getState() **безопасен** (логи, UI, чистые функции)
- Сравнительная таблица
- Практические правила и паттерны
- Правило ACID для эффектов

---

## 🎯 Быстрый старт

### Изучить приоритеты очередей:
```bash
# Базовые концепции
node examples/priority-example-1.js

# Эффекты через sample
node examples/priority-example-2.js

# Все приоритеты
node examples/priority-example-3-complex.js
```

### Понять проблемы с getState():
```bash
# ❌ Увидеть проблемы
node examples/priority-example-6-incorrect-getState.js

# ✅ Изучить решения
node examples/priority-example-7-correct-sample.js
```

---

## 🔑 Ключевые выводы

### 1. Система приоритетов (6 уровней)

```
0. child    → forward (deprecated)
1. pure     → map, on
2. read     → внутреннее чтение
3. barrier  → combine
4. sampler  → sample, guard
5. effect   → watch, обработчики эффектов
```

**Правило:** Чем ниже приоритет, тем раньше выполняется операция.

### 2. getState() vs Sample

| Критерий | getState() | sample |
|----------|-----------|--------|
| Race conditions | ❌ Не защищает | ✅ Защищает |
| Консистентность в async | ❌ Может измениться | ✅ Снимок не меняется |
| Множественные источники | ❌ Из разных моментов | ✅ Атомарный снимок |
| Отслеживание зависимостей | ❌ Нет в графе | ✅ Явные связи |

### 3. Главное правило

```
Если эффект ПРИНИМАЕТ РЕШЕНИЯ на основе состояния стора:
  → ВСЕГДА используйте sample
  → НИКОГДА не используйте getState()
```

### 4. Когда getState() безопасен

✅ Отладка и логирование
✅ Чтение для UI компонентов
✅ Синхронные чистые функции
✅ Чтение вне реактивного графа Effector

### 5. Когда getState() ОПАСЕН

❌ Асинхронные эффекты (async/await)
❌ Бизнес-логика с условиями (check-then-act)
❌ Конкурентные операции
❌ Транзакции (платежи, заказы, лимиты)

---

## 📚 Дополнительные материалы

### Официальная документация:
- [Computation Priority](../documentation/src/content/docs/en/explanation/computation-priority.md)
- [Sample](https://effector.dev/docs/api/effector/sample)
- [Store](https://effector.dev/docs/api/effector/store)
- [Effect](https://effector.dev/docs/api/effector/effect)

### Внутренняя реализация:
- [src/effector/kernel.ts](../src/effector/kernel.ts) - Система очередей
- [src/effector/step.ts](../src/effector/step.ts) - Создание шагов с приоритетами
- [src/effector/sample.ts](../src/effector/sample.ts) - Реализация sample
- [src/types/src/runner/heap.ts](../src/types/src/runner/heap.ts) - Skew heap для оптимизации

---

## 🤝 Вклад

Эти примеры созданы для помощи в понимании внутренней работы Effector. Если вы нашли ошибку или хотите улучшить примеры - создайте issue или pull request!

---

## 📝 Лицензия

Эти примеры распространяются под той же лицензией, что и Effector.

---

**Автор примеров:** Claude (Anthropic AI)
**Дата создания:** 2025-01-13
**Версия Effector:** 1.25.2
